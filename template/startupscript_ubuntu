# Ubuntu upstart file at /etc/init/mongodb-{{ .Name }}.conf

limit nofile 20000 20000
limit core 20000 20000

kill timeout 300 # wait 300s between SIGTERM and SIGKILL.

start on runlevel [2345]
stop on runlevel [06]

exec /usr/local/bin/mongod_{{ .Version }} --config {{ .DataPath }}/mongodb.conf